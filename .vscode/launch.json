{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run Main App",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run_refactored.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
